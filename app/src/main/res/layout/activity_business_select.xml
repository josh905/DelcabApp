<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rel"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/greyslash"
    tools:context="delcab.delcab.BusinessSelectActivity">

    <TextView
        android:id="@+id/chooseBusTxt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="34dp"
        android:fontFamily="sans-serif"
        android:inputType="none"
        android:text="Select your company/business"
        android:textAlignment="center"
        android:textColor="@color/colorPrimaryDark"
        android:textSize="26dp"
        android:textStyle="bold" />

    <Spinner
    android:layout_width="300dp"
    android:textAlignment="center"
    android:layout_marginTop="40dp"
    android:layout_below="@+id/chooseBusTxt"
    android:prompt="@android:color/white"
    android:background="@android:color/white"
    android:layout_centerHorizontal="true"
    android:layout_height="45dp"
    android:id="@+id/spinner"
    android:layout_weight="0.09" />

    <Button
    style="@style/Base.Widget.AppCompat.Button.Borderless"
    android:background="@drawable/green_button"
    android:text="Proceed"
    android:layout_width="290dp"
    android:layout_height="56dp"
    android:layout_below="@+id/spinner"
    android:layout_marginTop="40dp"
    android:id="@+id/proceedBtn"
    android:textSize="15sp"
    android:layout_centerHorizontal="true"
    android:textColor="@android:color/white" />

</RelativeLayout>